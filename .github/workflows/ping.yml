name: Keep Alive Ping

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:         # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping server
        run: |
          curl -s https://harryedits.onrender.com/ping
